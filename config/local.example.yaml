MESSAGING_CONFIG:
  TELEGRAM:
    TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN}
  BIRD:
    BIRD_API_URL: https://api.bird.com
    BIRD_ORGANIZATION_ID: ${BIRD_ORGANIZATION_ID}
    BIRD_WORKSPACE_ID: ${BIRD_WORKSPACE_ID}
    BIRD_API_KEY: ${BIRD_API_KEY}
    BIRD_SIGNING_KEY: ${BIRD_SIGNING_KEY}
    BIRD_CHANNEL_ID: ${BIRD_CHANNEL_ID}

DATABASE_CONFIG:
  SUPABASE:
    URL: ${SUPABASE_URL}
    KEY: ${SUPABASE_KEY}

BEHAVIORS_CONFIG:
  PROMPT: >
    You are Z-X9, an AI-powered robotic barista aboard the interstellar cruise ship "Cosmic Latte." 
    
    Your mission is to serve the most exotic and delicious beverages from across the galaxy to a 
    diverse clientele of aliens, humans, and interdimensional beings.
  ACTION_MANAGER:
    LLM_NAME: claude-3-sonnet-20240229
  INTENT_MANAGER:
    LLM_NAME: claude-3-sonnet-20240229
  MEMORY_MANAGER:
    LLM_NAME: claude-3-sonnet-20240229
  TASK_MANAGER:
    REDIS_URL: redis://127.0.0.1
  
WEB_ACCESS_CONFIG:
  GOOGLE:
    GOOGLE_API_KEY: ${GOOGLE_API_KEY}
    GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
    GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET}
    GOOGLE_AUTH_SCOPE:
      - https://www.googleapis.com/auth/calendar
      - https://www.googleapis.com/auth/drive.file
    CALENDAR:
      CALENDAR_NAME: Life Tracking
      CALENDAR_DESCRIPTION: Use this calendar to track what you do day to day.
    DRIVE:
      FOLDER_NAME: Life Tracking
      FILE_NAME_FORMAT: "%m_%d_%y %H:%M"



